项目名称：微信小程序记账与AI理财助手（应用名：昕辰记账）

【开发状态】全阶段完成 ✅（2026-07-01）
  项目已交付：P1 手动记账MVP + P2 粘贴识别 + P3 统计图表+规则管理+搜索 + P4 预算+CSV导入导出 + P5 AI智能分析 + P6 性能优化+异常处理+解析日志+使用指引
  备注①：原P2"转发消息解析"方案经真机验证不可行，改为粘贴识别方案
  备注②：统计图表使用Canvas 2D环形图+纯View柱状图（无ECharts依赖）
  备注③：AI使用DeepSeek流式输出+Markdown渲染，对话纯内存存储
  备注④：个人本地使用，跳过提审上线流程
  备注①：原P2"转发消息解析"方案经真机验证不可行，改为粘贴识别方案
  备注②：P3 统计图表未使用ECharts（避免~900KB依赖），改用Canvas 2D环形图+纯View柱状图（解决Canvas原生组件浮层问题）

项目背景与目标：解决个人日常消费记录繁琐、经常忘记记账的问题。利用微信小程序接收用户转发的微信支付通知消息，自动解析金额和商户信息实现快速记账，同时保留手动记账作为补充。引入 AI 智能分析模块，基于收支数据自动生成消费画像和钱包管理建议。所有记账数据 100% 保存在本地设备（wx.Storage），无需网络即可使用核心记账功能；AI 分析支持纯本地规则引擎离线运行，也支持用户自愿配置 API Key 调用云端大模型（仅上传脱敏聚合数据），保障个人隐私安全。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
一、功能需求
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 粘贴识别模块（微信支付消息快速记账）：✅ P2 已完成

    ⚠️ 方案变更说明：原设计为"用户转发支付消息→小程序接收 extraData 解析"，经真机验证，微信支付消息长按仅有「复制」「删除」选项，不存在「更多打开方式」。故改为粘贴识别方案。

    1.1 原理说明：用户在微信中长按支付消息 → 复制 → 打开「昕辰记账」小程序。App.onShow 中通过 wx.getClipboardData 自动检测剪贴板，若识别到微信支付凭证格式，首页弹出确认横幅；用户也可手动打开记账弹层点击「📋 粘贴识别」按钮触发；

    1.2 ✅ 小程序首页 onShow 时自动检测剪贴板，识别到支付消息格式（含"微信支付凭证"+"¥金额"）即弹出确认横幅；非支付内容不弹，不影响正常使用；

    1.3 ✅ 解析剪贴板文本内容，按行提取：首行→商户名、"微信支付凭证"→确认格式、"使用XX支付"→匹配账户、"¥XX.XX"→提取金额。同时通过关键词判断交易方向（消费/支付→支出，收款/退款/红包→收入）；

    1.4 ✅ 根据提取的商户名，匹配内置关键词映射表（rules），自动归入对应分类（如"面馆""鸡排""麦当劳"→餐饮，"滴滴""地铁"→交通）；

    1.5 ✅ 解析成功后弹出确认横幅（首页顶部绿色左边框卡片），展示商户名+金额，用户点击进入记账弹层（金额/分类/备注/账户已自动填入），可修改后保存或取消；

    1.6 ✅ 确认保存后记录存入 Storage，字段：金额、类型、分类、商户/描述（商户名）、交易时间、数据来源标记为"wechat"；

    1.7 ✅ 去重：app.js 记录上次剪贴板文本 `_lastClipText`，相同内容不重复弹横幅；

    1.8 ✅ 对于非支付格式的剪贴板内容，不弹横幅；用户手动点「粘贴识别」时若解析失败，toast 提示"未识别到支付信息"；

    1.9 ⏳ 使用指引：在记账弹层「📋 粘贴识别」旁有提示文字"复制微信支付消息后点这里"，无需额外引导弹窗。

2. 手动记账模块（用户主动记录）：✅ P1 已完成

    2.1 ✅ 用户点击首页底部中央"记一笔"悬浮按钮，弹出记账半弹层；
    2.2 ✅ 记账时需先选择分类（从已有分类中选择，如餐饮、交通、购物、娱乐、其他等），然后输入金额（数字键盘，保留两位小数），可选填写备注文字、选择账户（微信零钱/微信银行卡/现金/其他）、选择交易时间（默认当前时间，可改为过去时间补录）；
    2.3 ✅ 点击保存后数据存入本地 Storage，首页列表即时刷新显示新记录。保存按钮为绿底白字胶囊形；保存成功时展示勾选动画（500ms）+ 金额数字滚动动画（简易版）；
    2.4 ✅ 在首页最近记录列表中，用户可点击某条记录进入编辑页面，修改金额（数字键盘）、分类、备注、时间、账户等任意字段后保存；
    2.5 ✅ 在首页列表中，用户可左滑某条记录进行删除操作（QQ风格跟手拖拽+弹性吸边），弹出二次确认对话框（"确定删除这条记录？"），确认后从本地 Storage 移除；同时支持点击记录进入编辑页；
    2.6 ✅ 手动记账的数据来源字段统一标记为"manual"。

3. 分类与规则管理模块：✅ 全部完成

    3.1 ✅ 系统预置常用分类：餐饮、交通、购物、娱乐、医疗、教育、居住、服饰、通讯、其他，共10个默认分类，每个分类有默认名称和 Emoji 图标标识；
    3.2 ✅ 用户可在管理页面添加新的自定义分类（输入分类名称和自选 Emoji 图标即可）；
    3.3 ✅ 用户可查看和编辑关键词映射规则列表（添加/启用/禁用/删除规则），预置规则不可删除但可禁用，新增规则即时生效于粘贴识别；
    3.4 ✅ 用户可删除自定义分类（若分类下已有账单，提示用户先将相关账单重新归类或一并删除）。

4. 首页概览模块：✅ P1 已完成

    4.1 ✅ 首页顶部以大卡片展示本月总收入、总支出、本月结余（收入减去支出）三个核心数字；
    4.2 ✅ 展示今日收支简况：两个小卡片横向排列，显示今日支出最高的两个分类金额，若涉及超过 2 个分类则显示"其他 N 项 ¥XX"；若今日无支出则显示"暂无支出 🎉"；
    4.3 ✅ 首页顶部右侧提供搜索图标（🔍），点击跳转搜索页（P3已实现全文搜索+组合筛选）；
    4.4 ✅ 下方以列表形式展示最近交易记录（按交易时间倒序排列，每条显示：分类图标+名称、商户/备注摘要、金额（绿色收入/红色支出）、时间），默认显示最近 20 条，向下滑动加载更早记录（分页加载，每页 20 条）；
    4.5 ✅ 列表支持下拉刷新；
    4.6 ✅ 点击某条记录跳转到详情/编辑页面，可查看完整信息和修改。

5. 统计报表模块：✅ P3 已完成

    5.1 ✅ 统计页面顶部提供月份切换（左右箭头切换上一月/下一月，中间显示当前月份）；同时支持切换月/季/年视图（胶囊 Toggle），← → 箭头超过当前时间自动变灰；
    5.2 ✅ 以环形图（Canvas 2D 甜甜圈图）展示选定时间范围的支出/收入分类占比；中心标注总金额；支持支出/收入切换；
    5.3 ✅ 以纵向列表展示各类目支出排行（从高到低，显示排名、颜色点、分类图标、金额、交易笔数、占比百分比进度条）；
    5.4 ✅ 以柱状图（纯 WXML View 实现）展示选定范围内每日/月/季趋势，峰值柱深色高亮 + 金额标注；底部标注峰值信息；
    5.5 ✅ 支持按"支出"和"收入"分别查看统计，默认显示支出统计；汇总卡片同步切换。

6. 账户管理模块：✅ P1 已完成（消息解析关联账户 P2）

    6.1 ✅ 预置四个默认账户：微信零钱💳、微信银行卡🏦、现金💵、其他💰；
    6.2 ✅ 用户可添加新账户（输入账户名称，如"招行储蓄卡"）；
    6.3 🔶 每笔交易必须关联一个账户。消息解析入账时默认关联到"微信零钱"（→ P2），手动记账时由用户下拉选择（✅ 已实现）；
    6.4 ✅ 用户可删除账户（删除前需确保该账户下无账单，否则提示先转移账单）；
    6.5 ✅ 账户管理页列表展示账户图标+账户名，余额通过实时计算该账户下所有交易收支汇总得出，不单独存储。

7. 预算管理模块：✅ P4 已完成

    7.1 ✅ 用户可为总预算和各支出分类设置月度预算金额（单位为元，整数），点击弹出底部弹窗修改，设为 0 则删除预算；
    7.2 ✅ 首页概览卡片底部展示预算进度条（蜜桃红到鹅黄渐变），下方显示"预算使用 XX%"，通过 `getHomeData()` 传入 budgets 参数计算；
    7.3 ✅ 某分类支出超过该分类月度预算时，在首页以蜜桃红左边框通知条形式提醒（如"⚠️ 🍔餐饮超支¥200"）；
    7.4 ✅ 预算按 month 字段隔离，仅当月预算参与计算，月份切换后进度自动归零。

8. 交易搜索模块：✅ P3 已完成

    8.1 ✅ 首页顶部提供搜索图标🔍，点击跳转搜索页；
    8.2 ✅ 支持按以下维度搜索：关键词（备注/分类/账户）、类型（全部/支出/收入）、分类筛选（Picker）、日期范围（开始~结束）、金额范围（最低~最高）；筛选区可展开/收起；
    8.3 ✅ 搜索结果以列表展示，复用 transaction-item 组件，支持分页加载（每页20条）；
    8.4 ✅ 搜索条件可组合使用（多条件 AND 逻辑），无匹配时显示空状态「🔍 未找到匹配记录」。

9. 数据导出与备份模块：✅ P4 已完成

    9.1 ✅ 在设置页面「数据」分组提供 📤「导出 CSV」按钮，全部交易记录生成为 CSV（UTF-8 BOM + 7列表头），通过 `wx.shareFileMessage` 发送到聊天，失败降级复制剪贴板；
    9.2 ✅ 文件命名格式：昕辰记账_导出_20260701.csv；
    9.3 ✅ 提供 📥「导入 CSV」按钮：`wx.chooseMessageFile` 选择文件 → 逐行解析（支持引号转义）→ 分类名/账户名不区分大小写匹配现有数据 → 模糊前缀匹配 → 金额+时间+来源去重 → 批量写入；
    9.4 ✅ 导入完成后弹窗展示结果（成功 XX 条，跳过 XX 条重复）。

10. 设置模块：🔶 P1 设置页框架已完成，具体功能后续阶段实现

    10.1 ⏳ 使用指引：在设置页面顶部提供图文引导，教用户如何将微信支付消息转发到小程序；→ P2
    10.2 ⏳ 解析日志：展示最近接收到的微信支付消息原文列表和对应的解析结果；→ P2
    10.3 ✅ 数据管理：提供 📤「导出 CSV」+ 📥「导入 CSV」入口；"清空所有数据"按钮（红色文字，输入"确认清空"二次确认），执行后清除本地 Storage 中所有用户数据，保留预置分类和默认账户；
    10.4 ✅ AI 设置：Switch 切换本地规则/云端 API 模式；云端模式展开 API Key 输入行（密码模式+👁切换明文），实时加密保存；→ P5 已完成
    10.5 ✅ 关于：展示版本号（v1.0.0）和开源许可信息。

11. AI 智能分析与钱包管理建议模块：✅ P5 已完成

    11.1 ✅ 周期账单聚合：`engine.getAggregatedSummary()` 按月汇总收支总额、各分类占比等核心指标，生成结构化摘要数据作为 AI 分析的上下文；

    11.2 ✅ 消费习惯洞察：自动识别高频小额消费（7天内同类>3次）、超支分类、大额异常支出（单笔>近30天均值×3）；

    11.3 ✅ 智能建议生成：本地模式点击"生成报告"→基于规则链生成分析报告；云端模式→发送脱敏数据至 DeepSeek API 流式生成报告；

    11.4 ✅ AI 对话助手：输入框自由提问，本地模式根据问题关键词定向回答（排行/省钱/预算/结余），云端模式通过 DeepSeek 理解问题并流式回复；底部「📊 生成消费报告」一键生成月度报告；

    11.5 ✅ 本地规则引擎兜底：无 API Key 时自动使用 JS 规则引擎，根据问题类型给出定向建议，0.5s内返回；无网络也有基本智能反馈；

    11.6 ✅ 数据脱敏与隐私保护：云端模式下仅发送分类汇总数据（不含商户名/交易时间）；API Key 经纯 JS Base64+XOR 双重加密后存储；

    11.7 ⚠️ AI 建议历史：已简化——对话纯内存存储，切 Tab 不丢但退出小程序清空；历史页已移除；

    11.8 ✅ AI 账单标签：`tagTransaction()` 按规则自动标注（必要支出/可选支出/冲动消费/固定开销）。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
二、性能与安全需求
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

性能需求：
    - 小程序启动冷启动 < 2 秒，热启动 < 1 秒；
    - 消息解析在 500ms 内完成（含正则匹配+关键词分类）；
    - 本地 Storage 读写极快，1000 条记录下首页加载 < 300ms，统计图表计算 < 1 秒；
    - 代码包大小 < 2MB（微信小程序主包限制），分包后总大小 < 20MB；
    - 统计图表使用 Canvas 2D + 纯 View 实现，无 ECharts 依赖，无需分包；
    - CSV 导出 5000 条记录需在 5 秒内完成；
    - AI 云端接口调用时显示 loading 占位动画，响应超时 15 秒给予提示并可重试；
    - 本地规则引擎建议生成需在 0.5 秒内完成。

安全需求：
    - 所有记账数据 100% 存储在本地设备 wx.Storage 中，核心记账功能零网络通信；
    - 若用户主动填入 API Key 启用云端 AI，需明确弹窗告知用户数据将发送至第三方大模型服务商，且仅发送脱敏聚合数据（不包含商户名、交易时间、个人身份信息等）；
    - API Key 加密后存储在本地 Storage，绝不硬编码在代码中；
    - 遵循最小权限原则，小程序不申请任何多余 scope（如地理位置、通讯录等）；
    - 导出 CSV 时不包含任何设备标识或用户身份信息；
    - 消息转发解析仅处理用户主动转发的内容，不读取用户聊天记录。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
三、技术架构与项目要求
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 开发语言与框架：
    1.1 开发语言：JavaScript（ES6+）
    1.2 框架：微信小程序原生框架（WXML + WXSS + JS）
    1.3 基础库版本要求：最低 2.20.0（支持 shareDataMessage 等消息转发 API）
    1.4 开发工具：微信开发者工具（官方 IDE）+ VS Code 辅助编辑
    1.5 注意：不使用 TypeScript、不使用 Taro/uni-app 等跨端框架，保持技术栈最简

2. 界面框架：
    2.1 使用微信小程序原生 WXML + WXSS 编写界面
    2.2 WXSS 设计基准：iPhone 6 屏幕宽度 375pt = 750rpx（1pt = 2rpx）
    2.3 保留马卡龙萌系视觉风格（见第五节视觉规范），WXSS 自定义属性（CSS 变量）实现全局主题色
    2.4 仅实现浅色主题

3. 架构模式：
    3.1 采用简单的"页面-服务-工具"三层分离：
        - View 层：WXML 模板 + WXSS 样式
        - Logic 层：Page/Component 的 JS，调用 services 层
        - Data 层：services/ 封装 wx.Storage 读写操作
    3.2 全局状态：使用 App.globalData 存储当前用户配置，启动时从 Storage 加载到内存
    3.3 不使用 mobx / redux 等状态管理库

4. 本地数据存储（wx.Storage）：

    使用 wx.setStorageSync / wx.getStorageSync 系列 API。数据以 JSON 序列化存储：

    Storage Key              | 存储内容             | 数据结构
    ─────────────────────────────────────────────────────
    transactions             | 所有交易记录数组       | Array<Transaction>
    categories               | 分类列表              | Array<Category>
    accounts                 | 账户列表              | Array<Account>
    rules                    | 关键词映射规则列表      | Array<Rule>
    parse_logs               | 消息解析日志数组       | Array<ParseLog>
    budgets                  | 预算设置              | Array<Budget>
    ai_reports               | AI 建议历史           | Array<AiReport>
    app_config               | 应用配置              | Object

    各数据结构定义：

    Transaction {
      id: String,
      amount: Number,
      type: String,          // "income" | "expense"
      categoryId: String,
      categoryName: String,
      accountId: String,
      accountName: String,
      description: String,   // 商户/备注
      note: String|null,
      source: String,        // "wechat" | "manual"
      timestamp: Number,     // 交易时间戳（毫秒）
      createdAt: Number,     // 记录创建时间戳
      aiTag: String|null     // AI 标签：必要支出/可选支出/冲动消费/固定开销
    }

    Category {
      id: String,
      name: String,
      emoji: String,
      isPreset: Boolean,
      sortOrder: Number
    }

    Account {
      id: String,
      name: String,
      isPreset: Boolean
    }

    Rule {
      id: String,
      keyword: String,
      categoryId: String,
      isPreset: Boolean,
      enabled: Boolean
    }

    ParseLog {
      id: String,
      rawText: String,
      amount: Number|null,
      type: String|null,
      categoryName: String|null,
      status: String,        // "success" | "failed"
      failReason: String|null,
      timestamp: Number
    }

    Budget {
      id: String,
      categoryId: String|null,  // null 表示总预算
      amount: Number,
      month: String             // "2026-07"
    }

    AiReport {
      id: String,
      type: String,          // "report" | "chat"
      role: String,          // "user" | "assistant"
      content: String,       // Markdown 格式
      summary: String|null,  // 前 100 字摘要
      month: String|null,    // 关联月份 "2026-07"
      aiModel: String|null,  // "local_engine" 或具体模型名
      timestamp: Number
    }

    ⚠️ wx.Storage 限制：单个 key 数据上限 1MB，总上限 10MB。
    建议：在设置页面醒目提示用户定期导出 CSV 备份。

5. 页面路由设计：

    5.1 底部 TabBar（自定义），共 4 个 Tab：
        - Tab1: 首页（pages/home/home）—— 概览卡片 + 今日速览 + 最近记录
        - Tab2: 统计（pages/stats/stats）—— 图表 + 排行 + 趋势
        - Tab3: AI助手（pages/ai/ai）—— 对话界面 + 报告生成
        - Tab4: 我的（pages/settings/settings）—— 设置项列表

    5.2 底部 TabBar 中央悬浮"记一笔"按钮（cover-view），点击弹出记账半弹层

    5.3 其他子页面路由（wx.navigateTo）：
        - pages/category/manage      → 分类管理
        - pages/rules/manage         → 规则管理
        - pages/account/manage       → 账户管理
        - pages/transaction/edit     → 交易编辑/详情（参数: id）
        - pages/budget/set           → 预算设置
        - pages/search/index         → 搜索页
        - pages/logs/index           → 解析日志页
        - pages/ai/history           → AI 历史建议页

6. 项目目录结构：

    miniprogram/
    ├── app.js                    // 小程序入口（处理转发消息）
    ├── app.json                  // 全局配置（tabBar、路由、窗口样式）
    ├── app.wxss                  // 全局样式（CSS 变量、公共类）
    ├── pages/
    │   ├── home/                 // 首页
    │   ├── stats/                // 统计页
    │   ├── ai/                   // AI助手页
    │   │   ├── ai.wxml/wxss/js/json
    │   │   └── history.wxml/wxss/js/json   // AI 历史建议页（分包内）
    │   ├── settings/             // 设置页
    │   ├── transaction/          // 记账/编辑页
    │   ├── category/             // 分类管理页
    │   ├── account/              // 账户管理页
    │   ├── rules/                // 规则管理页
    │   ├── budget/               // 预算页
    │   ├── search/               // 搜索页
    │   └── logs/                 // 解析日志页
    ├── components/               // 公共组件
    │   ├── fab/                  // 悬浮按钮组件
    │   ├── transaction-item/     // 交易列表项组件
    │   ├── add-sheet/            // 记账半弹层组件
    │   ├── empty-state/          // 空状态占位组件
    │   ├── chart/                // Canvas 2D 图表组件（环形图）
    │   └── ai-bubble/            // AI 对话气泡组件
    ├── services/                 // 业务逻辑层
    │   ├── storage.js            // wx.Storage 读写封装
    │   ├── parser.js             // 微信支付消息正则解析
    │   ├── export.js             // CSV 导出/导入
    │   ├── calculator.js         // 汇总计算（月度收支、分类占比等）
    │   ├── engine.js             // 本地 AI 规则引擎
    │   └── ai.js                 // 云端 AI 接口调用（wx.request）
    └── utils/                    // 工具函数
        ├── format.js             // 金额格式化、时间格式化
        ├── constants.js          // 常量（预置分类、关键词映射表等）
        └── request.js            // 封装 wx.request Promise 化

7. 第三方依赖：
    7.1 无 ECharts 依赖：统计图表使用 Canvas 2D（环形图）+ 纯 WXML View（柱状图）实现，无需分包
    7.2 可选：towxml —— Markdown 渲染组件（AI 报告展示），约 150KB，放入分包
    7.3 不引入其他第三方 npm 包

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
四、分阶段开发计划
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

第一阶段：注册 + 搭架子 + 手动记账 MVP
    □ 注册微信小程序账号（个人主体），获取 AppID
    □ 安装微信开发者工具，创建原生小程序项目
    □ 搭建自定义 tabBar（4个Tab）+ 页面骨架
    □ 实现 Storage 读写服务层
    □ 实现手动记账功能：FAB → 记账半弹层 → 选分类/输金额 → 保存到 Storage
    □ 实现首页交易记录列表（倒序，分页，左滑删除）
    □ 实现分类和账户管理页面
    □ 真机预览，验证手动记账全流程

第二阶段：微信支付消息解析
    □ 编写微信支付消息正则解析器（内置预置关键词规则表）
    □ 实现 App.onShow 中接收转发消息的逻辑
    □ 实现解析确认半弹层（用户确认/修改/取消）
    □ 实现去重逻辑
    □ 实现解析日志记录和查看页面
    □ 实现首次使用引导图文
    □ 真机测试：实际转发几笔微信支付消息，验证解析准确率

第三阶段：统计图表 + 规则管理 ✅ 已完成
    ✅ Canvas 2D 实现环形图（支出/收入分类占比甜甜圈）
    ✅ 纯 View 实现柱状趋势图（每日/月/季支出趋势，峰值高亮）
    ✅ 实现月/季/年切换 + 支出/收入切换
    ✅ 实现规则管理页面（关键词映射的增删改 + 启用/禁用开关）
    ✅ 实现搜索页面（关键词 + 组合筛选 + 分页结果）

第四阶段：预算 + 导入导出 ✅ 已完成
    ✅ 实现预算设置（总预算+分类预算）+ 首页进度条 + 超支提醒通知条
    ✅ 实现 CSV 导出（UTF-8 BOM, wx.shareFileMessage 分享到聊天）
    ✅ 实现 CSV 导入（分类名/账户名智能匹配, 去重, 批量写入）
    ✅ 交易编辑/删除（P1 已完成）

第五阶段：AI 智能分析 ✅ 已完成
    ✅ 实现本地 JS 规则引擎（超支检测+高频消费+异常支出+问题感知定向回答）
    ✅ 实现周期账单聚合（getAggregatedSummary）
    ✅ 实现 AI 设置（Switch 切换+API Key 密码输入+👁切换明文+加密存储）
    ✅ 实现云端 DeepSeek API 流式调用（SSE 解析+UTF-8 解码+enableChunked）
    ✅ 实现 AI 对话助手页面（流式输出+Markdown 渲染+生成报告+欢迎状态）
    ⚠️ AI 历史已简化移除（对话纯内存，切 Tab 不丢）

第六阶段：打磨上线 ✅ 已完成
    ✅ 异常处理（Storage超限提示+AI网络降级+编辑页参数保护+空数据兼容）
    ✅ 性能优化（Storage内存缓存+dataVersion脏标记+单次遍历计算）
    ✅ 首页卡片入场动画（slideUp 300ms）
    ✅ 解析日志（parser自动记录+pages/logs查看历史）
    ✅ 使用指引（设置页弹窗展示操作步骤）
    ⏭ 分包配置（无ECharts等大依赖，无需分包）
    ⏭ 提审上线（个人本地使用，跳过）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
五、界面视觉设计规范
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

设计风格：马卡龙萌系（大圆角、暖粉白背景、樱花粉主色、蜜桃红/薄荷绿），Emoji 图标替代图标库。

设计基准：iPhone 6 屏幕宽度 375pt = 750rpx，下文中所有尺寸标注为 rpx。

1. 色彩体系

    主色 Primary：#F8BBD0 樱花粉 → TabBar 选中态、FAB、关键按钮、进度条
    辅色 Secondary：#B2EBF2 薄荷蓝 → 分类图标背景、卡片辅助底色
    辅色 Tertiary：#FFF9C4 鹅黄 → AI 建议气泡、高亮标签
    支出色 Expense：#FF8A80 蜜桃红 → 支出金额、超支标记
    收入色 Income：#A5D6A7 薄荷绿 → 收入金额
    背景 BG：#FFF5F7 暖粉白 → 全页背景色
    卡片 Card：#FFFFFE 近白 → 卡片底色
    文字 Primary：#5D4037 深咖 → 标题和大数字
    文字 Secondary：#8D6E63 浅咖 → 副标题、备注、时间戳
    分割线：#F0E6E8 淡粉灰 → 列表分割

    WXSS 全局变量（app.wxss）：
    ```css
    page {
      --color-primary: #F8BBD0;
      --color-secondary: #B2EBF2;
      --color-tertiary: #FFF9C4;
      --color-expense: #FF8A80;
      --color-income: #A5D6A7;
      --color-bg: #FFF5F7;
      --color-card: #FFFFFE;
      --color-text-primary: #5D4037;
      --color-text-secondary: #8D6E63;
      --color-divider: #F0E6E8;
      background-color: var(--color-bg);
    }
    ```

2. 形状规范

    页面卡片：圆角 48rpx
    按钮：圆角 40rpx
    分类图标容器：圆形，直径 96rpx
    TabBar：自定义组件，顶部圆角 48rpx
    输入框：圆角 32rpx
    AI 对话气泡：圆角 40rpx（用户气泡右对齐白底，AI 气泡左对齐鹅黄底）

3. 字体规范

    首页结余大数：72rpx，font-weight: 700，深咖
    卡片标题：28rpx，font-weight: 400，浅咖
    卡片数字：40rpx，font-weight: 700，收入薄荷绿/支出蜜桃红
    列表商户名：32rpx，font-weight: 400，深咖
    列表时间/备注：24rpx，font-weight: 400，浅咖
    按钮文字：32rpx，font-weight: 700，白色
    TabBar 标签：22rpx，font-weight: 400
    AI 对话正文：28rpx，font-weight: 400，深咖，line-height: 1.5

    ⚠️ WXSS 字重仅支持 400(normal) 和 700(bold)。

4. 动效规范

    页面切换：微信原生右滑进入/退出动画
    首页卡片入场：CSS animation 从下方滑入 + 淡入（300ms）
    FAB 点击：CSS transition，scale(1) → scale(0.88) → scale(1)
    保存成功：勾选图标 + 文字提示（500ms）
    数字变化：JS setInterval 实现 countUp（800ms）
    AI 回复加载：三点跳动指示器（CSS animation-delay 错开）

5. 各页面 UI 详细规范

    5.1 底部 TabBar（全局）
        - 自定义 tabBar（app.json 中 "custom": true）
        - 白色背景 #FFFFFE，顶部圆角 48rpx，底部直角
        - 高度 128rpx
        - 中央预留 112rpx 宽区域，用于悬浮 FAB 覆盖
        - 四个 Tab：首页（🏠）、统计（📊）、AI助手（🤖）、我的（👤）
        - 选中樱花粉，未选浅咖；选中态下方 8rpx 小圆点指示器
        - 中央 FAB：直径 112rpx，樱花粉背景，白色 + 号

    5.2 首页（pages/home/home）
        问候区：左侧"嗨，今天也想省钱吗？"深咖 28rpx，下方"X月已记 X 天 🔥"浅咖 24rpx
        右侧搜索图标 🔍 48rpx

        概览卡片：白色圆角 48rpx，margin 32rpx
        上方"本月结余"浅咖 28rpx，中间"¥ X,XXX.XX"深咖 72rpx Bold 居中
        下方左右"收入 ¥X"和"支出 ¥X"，竖线分隔，收入薄荷绿/支出蜜桃红
        底部预算进度条（高 16rpx，圆角 8rpx，蜜桃红到鹅黄渐变）

        今日速览：两个卡片横向 flex 排列，各 flex:1，间距 24rpx，圆角 32rpx，薄荷蓝 20% 背景
        若今日无支出显示"暂无支出 🎉"

        最近记录列表：
        标题栏"最近记录"深咖 32rpx，右侧"查看全部 →"浅咖 24rpx
        列表项：左分类图标（96rpx 圆形）+ 中商户名 32rpx/时间 24rpx + 右金额 32rpx
        左滑删除
        底部预留 160rpx 避开 FAB

    5.3 统计页（pages/stats/stats）
        顶部胶囊 Toggle（月/季/年），选中樱花粉背景白字
        左右箭头 + 时间标签切换
        Canvas 2D 甜甜圈环形图，中心标注总金额
        分类排行列表 + 纯 View 柱状趋势图（峰值柱深色高亮+金额标注）

    5.4 AI助手页（pages/ai/ai）
        顶部栏：
        标题"✨ 智能建议"深咖 36rpx Bold
        右侧时钟图标 🕐（跳转 AI 历史建议页）

        对话内容区：
        可滚动 scroll-view，最新消息在底部
        AI 建议气泡：鹅黄背景 #FFF9C4，圆角 40rpx，文字深咖 28rpx line-height 1.5，padding 24rpx
        建议卡片（报告模式）：白色卡片圆角 32rpx，左侧 8rpx 樱花粉竖线，标题深咖 28rpx + 正文浅咖 26rpx
        用户消息气泡：白色背景，圆角 40rpx，右对齐，文字深咖 28rpx
        加载中气泡：鹅黄背景内三点跳动动画（三个直径 16rpx 圆点，依次缩放，间隔 200ms）
        首次进入无历史时显示欢迎气泡："嗨~ 我是你的 AI 理财小助手，先记几天账，我就知道怎么帮你省钱啦 ✨"鹅黄背景居中

        生成报告按钮：
        全宽樱花粉按钮圆角 40rpx，文字"✨ 生成我的消费报告"白色 32rpx
        点击后 loading 转圈，2-3 秒后逐条弹出建议气泡

        底部输入框：
        固定在底部，白色背景圆角 48rpx，placeholder"问问 AI..."浅咖 28rpx
        右侧 Send 图标 48rpx 樱花粉
        首次云端 API 发送前弹 Dialog"将发送脱敏数据至 AI，是否继续？"

    5.5 我的页（pages/settings/settings）
        顶部渐变卡片（樱花粉 → 薄荷蓝），内部"我的记账"+ "本地数据，安全可靠 🔒"
        分组列表：
        - "使用"组：使用指引（转发教学）、解析日志
        - "数据"组：分类管理、规则管理、账户管理、预算设置、搜索记录、导出/导入 CSV
        - "AI"组：Toggle 本地规则/云端 API，开启云端展开 API Key 输入（密码模式+眼睛图标）+ 模型下拉选择
        - "其他"组：清空数据（红色）、关于（版本号）
        每行高 112rpx，左图标 48rpx，右箭头

    5.6 记账半弹层（全局共用）
        触发：点击中央 FAB
        拖拽把手居顶（80rpx × 8rpx，圆角 32rpx）
        分类选择：两行各 4 个圆形图标网格，96rpx 直径
        金额显示：居中 72rpx 深咖，默认"¥0"
        数字键盘：4×4 网格（1-9 + . + 0 + ⌫），按键圆角 32rpx
        可选输入（折叠）：备注输入框 + 账户下拉
        保存按钮：全宽樱花粉圆角 40rpx

    5.7 子页面通用规范
        自定义导航栏（透明背景，深咖返回箭头 + 标题）
        内容区白色卡片圆角 48rpx
        - 分类管理：列表 + 底部"添加分类"（名称+Emoji）
        - 规则管理：列表关键词→分类，底部"添加规则"
        - 账户管理：列表图标+名称+实时余额
        - 交易编辑：金额/分类/账户/时间/备注/来源标签
        - 预算设置：总预算 + 分类预算列表
        - 搜索页：搜索栏 + FilterChip + 结果列表，空状态"没有找到相关记录 🔍"
        - 日志页：解析日志列表，失败项标蜜桃红 Tag"未识别"
        - AI 历史：时间倒序列表，卡片日期+类型+摘要+AI 模型标签，点击展开完整 Markdown

6. 交互规范汇总
    - 子页面左上角返回箭头，点击 wx.navigateBack
    - 列表用 scroll-view，wx:key 绑定 id
    - 删除操作均二次确认 wx.showModal
    - 金额统一两位小数 + 千位分隔符：¥1,234.56
    - 时间戳显示：今日"今天 HH:mm"，昨日"昨天 HH:mm"，今年"MM月dd日"，往年"yyyy年MM月dd日"
    - 分类默认 Emoji：餐饮🍔、交通🚇、购物🛍️、娱乐🎮、医疗💊、教育📚、居住🏠、服饰👗、通讯📱、其他📦
    - 默认账户 Emoji：微信零钱💳、微信银行卡🏦、现金💵、其他💰

7. 微信小程序特有约束
    - 自定义 tabBar 在 app.json 中配置 "tabBar": {"custom": true}
    - AI 云端 API 域名需在「微信公众平台 → 开发管理 → 服务器域名」配置白名单（如 api.deepseek.com）
    - 页面层级最多 10 层（wx.navigateTo 最多 10 次）
    - WXSS 不支持 backdrop-filter、position:fixed 在 scroll-view 内受限
    - 主包 2MB 上限，总包（含分包）20MB 上限
    - wx.Storage 单个 key 上限 1MB，总上限 10MB
    - 清除小程序数据会导致所有本地数据丢失 → 提醒用户定期导出 CSV
    - 微信小程序不允许使用 SSE（Server-Sent Events），云端 AI 需使用普通 HTTP 请求或 WebSocket 实现流式输出；如流式实现困难，可先用非流式（一次性返回完整响应）
